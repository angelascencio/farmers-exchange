<section id="blogposts" class="container">
    <h1>All Posts</h1>

    {{#each posts as |posts|}}
     
    <div class="gig">
      <h3>{{product_name}}</h3>
      <p>{{description}}</p>
      
    </div>


    {{else}}

    <p>No posts available</p>

    {{/each}}

   
   
  </section>